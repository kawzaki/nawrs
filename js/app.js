$(document).foundation();

// autocomplete UI 
$( document ).ready(function() {	
	// $.getJSON( 'search/autosuggest/?callback=1', function( data ) {
		// automplete_cache = $.map(data, function(el) { return el; })
		
		// $( "#searchTerms" ).autocomplete({
		  // autoFocus: true,
		  // appendTo: $(".ui-front"),
		  // minLength: 2,
		  //source: automplete_cache
		  // source: function(request, response) {
			  // var results = $.ui.autocomplete.filter(automplete_cache, request.term);
			  // response(results.slice(0, 10));
			  // }		  
		// });
	// });
	
});

